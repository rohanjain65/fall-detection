import torch

backbone = "vit_b_16"
num_classes = 2  
model = FallDetectionModel(backbone=backbone, num_classes=num_classes, pretrained=False)


input_tensor = torch.randn(4, 3, 224, 224)

output = model(input_tensor)

print("Output shape:", output.shape)